import React from 'react';

const TopicDetails = ({topics}) => {
    console.log(topics);
    return (
        <div>
            <h1>topic details</h1>
        </div>
    );
};

export default TopicDetails;